# Database migrations

Versioned TypeORM migrations live in this directory.

Stage 4-B intentionally contains no business tables or schema migration.
Generate the first migration only after the Stage 2 schema is approved for
implementation.
